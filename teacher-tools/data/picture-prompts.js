/*
  Built-in Picture Prompt metadata.
  Add objects with: id, title, category, and src (a browser-accessible image path).
  The interface deliberately shows an empty state until real content is added.
*/
globalThis.INKY_PAWS_PICTURE_PROMPTS = [];
